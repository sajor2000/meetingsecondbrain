{
  "name": "meeting-app",
  "private": true,
  "scripts": {
    "convex:dev": "npm --prefix convex run dev",
    "convex:check": "npm run convex:typecheck && npm run convex:codegen",
    "convex:codegen": "sh -c 'if [ -n \"${CONVEX_DEPLOYMENT:-}\" ]; then npm --prefix convex exec -- convex codegen --dry-run --typecheck enable; else echo \"Skipping Convex codegen dry-run because CONVEX_DEPLOYMENT is not configured.\"; fi'",
    "convex:typecheck": "npm --prefix convex run typecheck",
    "convex:test": "npm --prefix convex test",
    "xcodegen": "xcodegen generate",
    "swift:build:core": "swift build --package-path packages/RecallOSCore",
    "swift:test:core": "swift test --package-path packages/RecallOSCore",
    "xcode:test:shared": "xcodebuild test -project RecallOS.xcodeproj -scheme RecallOSSharedTests -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO",
    "xcode:test:macos": "npm run xcode:test:shared",
    "xcode:test:ios": "scripts/xcode-test-ios.sh",
    "xcode:test": "npm run xcode:test:macos && npm run xcode:test:ios",
    "xcode:build:macos": "xcodebuild -project RecallOS.xcodeproj -scheme RecallOSMac -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO build",
    "xcode:build:ios": "xcodebuild -project RecallOS.xcodeproj -scheme RecallOSiOS -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build",
    "xcode:build": "npm run xcode:build:macos && npm run xcode:build:ios",
    "phase1:check:local": "npm run convex:check && npm run swift:build:core",
    "phase1:check": "npm run convex:check && npm run convex:test && npm run swift:test:core && npm run xcode:test && npm run xcode:build"
  },
  "dependencies": {
    "convex": "^1.29.3"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
